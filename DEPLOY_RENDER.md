# Render Cloud 部署教學

本教學將指導您如何將「電腦基本功」遊戲網站部署到 Render Cloud。

## 📋 前置準備

1. **GitHub 帳號**：需要將程式碼上傳到 GitHub
2. **Render 帳號**：前往 [render.com](https://render.com) 註冊（可使用 GitHub 帳號登入）

## 🚀 部署步驟

### 步驟 1：準備 GitHub Repository

1. **在 GitHub 創建新 Repository**
   - 前往 [GitHub](https://github.com/new)
   - Repository 名稱：例如 `computer-basics-games`
   - 設為 Public（免費方案需要）
   - 不要初始化 README（因為我們已經有文件了）

2. **上傳程式碼到 GitHub**
   ```bash
   # 在 share 目錄下初始化 git
   cd "d:\桌面\website\web\templates\share"
   git init
   git add .
   git commit -m "Initial commit: 電腦基本功遊戲網站"
   
   # 連接 GitHub repository
   git remote add origin https://github.com/你的用戶名/computer-basics-games.git
   git branch -M main
   git push -u origin main
   ```

### 步驟 2：執行 setup_static.py 複製資源

**重要**：在推送到 GitHub 之前，必須先執行 `setup_static.py` 複製靜態資源！

```bash
cd "d:\桌面\website\web\templates\share"
python setup_static.py
```

確認 `static/` 目錄已創建並包含所有資源後，再執行 git 操作。

### 步驟 3：在 Render 創建 Static Site

1. **登入 Render**
   - 前往 [render.com](https://render.com)
   - 使用 GitHub 帳號登入

2. **創建新 Static Site**
   - 點擊 Dashboard 右上角的 **"New +"** 按鈕
   - 選擇 **"Static Site"**

3. **連接 GitHub Repository**
   - 選擇 **"Connect a repository"**
   - 選擇您剛才創建的 GitHub repository
   - 如果沒看到，點擊 **"Configure account"** 授權 Render 存取您的 GitHub

4. **配置設定**
   - **Name**: `computer-basics-games`（或您喜歡的名稱）
   - **Branch**: `main`（或您的預設分支）
   - **Root Directory**: 留空（因為所有文件都在根目錄）
   - **Build Command**: 留空（靜態網站不需要建置）
   - **Publish Directory**: 留空（所有文件都在根目錄）

5. **進階設定（可選）**
   - 點擊 **"Advanced"** 展開進階選項
   - **Environment**: 選擇 `Production`
   - **Pull Request Previews**: 可開啟（預覽 PR 變更）

6. **部署**
   - 點擊 **"Create Static Site"**
   - Render 會自動開始部署

### 步驟 4：設定自訂網域（可選）

1. 在 Render Dashboard 中，點擊您的 Static Site
2. 前往 **"Settings"** 標籤
3. 在 **"Custom Domains"** 區塊：
   - 點擊 **"Add Custom Domain"**
   - 輸入您的網域（例如：`games.yourdomain.com`）
   - 按照指示設定 DNS 記錄

## ⚙️ 配置說明

### 基本配置

Render 的 Static Site 服務會自動：
- 提供 HTTPS 憑證（免費）
- 自動部署（當您 push 到 GitHub 時）
- 提供免費的 `.onrender.com` 子網域

### 預設首頁

Render 會自動尋找以下文件作為首頁（依優先順序）：
1. `index.html`
2. `index.htm`

我們的專案已經有 `index.html`，所以會自動作為首頁。

## 🔄 更新部署

每次更新程式碼時：

1. **修改本地文件**
2. **執行 setup_static.py**（如果有新增或修改靜態資源）
3. **推送到 GitHub**
   ```bash
   git add .
   git commit -m "更新說明"
   git push
   ```
4. **Render 會自動重新部署**（通常需要 1-2 分鐘）

## 📝 重要注意事項

### 1. 靜態資源必須包含

確保 `static/` 目錄已包含所有資源：
- ✅ 執行 `setup_static.py` 後再推送到 GitHub
- ✅ 確認 `static/` 目錄在 git 中（不要加入 `.gitignore`）

### 2. 路徑檢查

所有 HTML 文件中的路徑都應該是相對路徑：
- ✅ `../static/` （在 games/ 目錄下的文件）
- ✅ `static/` （在根目錄的文件）

### 3. 檔案大小限制

- Render 免費方案：單一檔案最大 100MB
- 如果遊戲檔案太大，考慮使用 CDN 或外部儲存

### 4. 環境變數

靜態網站不需要環境變數，但如果未來需要：
- 在 Render Dashboard → Settings → Environment Variables 設定

## 🐛 常見問題

### Q: 部署後看不到網站？

**A:** 檢查以下項目：
1. 確認 `index.html` 存在於根目錄
2. 檢查 Render 的 Build Log 是否有錯誤
3. 確認所有路徑都是相對路徑

### Q: 圖片或資源載入失敗？

**A:** 
1. 確認 `static/` 目錄已上傳到 GitHub
2. 檢查 HTML 中的路徑是否正確（相對路徑）
3. 在瀏覽器開發者工具中檢查 404 錯誤

### Q: 如何查看部署日誌？

**A:**
1. 在 Render Dashboard 中點擊您的 Static Site
2. 前往 **"Logs"** 標籤
3. 查看最新的部署日誌

### Q: 如何回滾到之前的版本？

**A:**
1. 在 Render Dashboard → **"Manual Deploy"**
2. 選擇之前的 commit
3. 點擊 **"Deploy"**

## 📊 監控與分析

### Render 提供的功能

- **部署歷史**：查看所有部署記錄
- **即時日誌**：監控網站狀態
- **效能監控**：查看回應時間和錯誤率

### 建議加入的分析工具

- Google Analytics
- Google Search Console
- 其他網站分析工具

## 🔒 安全性

1. **HTTPS**：Render 自動提供免費 SSL 憑證
2. **CORS**：如果需要，可在 HTML 中設定 CORS headers
3. **內容安全策略**：可在 HTML 的 `<head>` 中加入 CSP meta tag

## 💰 費用

- **免費方案**：
  - 無限靜態網站
  - 自動 HTTPS
  - 自動部署
  - 100GB 頻寬/月
  - 適合個人專案和小型網站

- **付費方案**：
  - 更多頻寬
  - 優先支援
  - 自訂網域（免費方案也支援）

## 📚 相關資源

- [Render 官方文件](https://render.com/docs)
- [Render Static Sites 文件](https://render.com/docs/static-sites)
- [GitHub 文件](https://docs.github.com)

## 🎉 完成！

部署完成後，您會獲得一個類似這樣的網址：
`https://computer-basics-games.onrender.com`

恭喜！您的遊戲網站已經上線了！🎮
